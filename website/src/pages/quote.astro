---
// The quote form now lives on /contact. Anything still hitting /quote
// (old bookmarks, search engines, direct links) gets a 301 to the new home.
return Astro.redirect("/contact#quote-form", 301);
---
